# Backlog

Known issues carried over from Cagdas's personal vault backlog on 2026-08-27.
They were living in a wiki where no build ever saw them.
Each entry keeps its original date. Detail for most lives in that vault's `_log.md` under the same date.

- 2026-08-07 — fix — [tela] **Free-tier quotas meter the wrong unit** — `max_atlas_sources`/`max_llm_calls_per_month` count sources and calls, but cost lives in repo *size*: the 2026-08-06 user was inside both limits while spending 7.05M embed tokens and 35 min of tardis GPU. Wants cost-shaped columns enforced at `acquire`. Numbers unconfirmed. `_log` 2026-08-07.
- 2026-08-07 — fix — [tela] **67% of Atlas chunks are truncated before embedding** — `rag.maxEmbedChars = 1600` is tuned for the retired mxbai (512-token ctx); run 92 measured avg 3,398 chars with 3,980 of 5,944 over the clamp. Two-thirds of the corpus has vectors built from under half their text. Raise for qwen3-embedding and re-embed.
- 2026-08-07 — fix — [tela] **`Usage.EmbedTokens` counts raw input, not what was embedded** — overstates by ~`raw/1600`, so the 3M/mo cap can block a free user on tokens never spent. Meter the clamped length, then re-check the cap.
- 2026-08-13 — fix — [tela] **A dead Atlas source credential is invisible to everyone** — source 16 has failed ~92×/day since 11 Jul with no UI state, no mail, no metric; `atlas_sources` has no `last_error`/`last_ok_at` to hang a badge on. `_log` 2026-08-13.
- 2026-08-17 — fix — [tela] **Atlas's 15-min drift probe isn't gated on the entitlement that gates the refresh** — ~640 failed probes/day on seven sources that will never regenerate. Apply `scheduledAtlasAllowed()` in the detect path too.
- 2026-06-10 — fix — [tela] **Yjs-doc ↔ `pages.body` divergence (general class).** Body is only written by the leader's local-edit saves, so peer edits and drawings never persist → edit mode shows them, view/search/export don't. Excalidraw was point-patched (`db43d88`); the broad class is open. Durable fix = server-side Yjs→markdown on the WS server. ⛔ Do NOT "auto-heal by diffing on open" — tried, write-amplification loop (milkdown parse∘serialize isn't a fixed point). #65 collab-rework area.
- 2026-08-16 — fix — [infra/tela-seo] **A trailing slash is a hard 404 on telawiki public pages** — `/compare/notion` redirects *to* the slash, but `/cagdassalur/blog/` 404s. Marketing and public halves take opposite conventions; a shared link that picks up a slash dies.
- 2026-07-01 — chore — [tela] **Lower `TELA_LLM_MAX_INFLIGHT` 20 → ~8** in hermes `deploy/.env`. A 24-concurrent load test melted L1 (embed+rerank+chat share one tardis GPU); the spill mechanism works, the threshold is just loose. Tune from `tela_llm_foreground_spill_total`.
- 2026-06-10 — cleanup — [tela] **Refresh the mira tech-debt report** (mira.cagdas.io/p/tela-tech-debt) — the whole "Fix now" section and most quick wins shipped, so the page reads as live bugs that aren't. Carries the R1–R8 opportunistic-refactor pile too; do those as areas get touched.
- 2026-06-10 — fix — [tela] **Docs contradict the code in three places**: `docs/architecture.md`'s MCP section narrates the retired pre-v0.7 TS client; `docs/api.md` is a wrong third copy of the contract (12 of ~199 routes); `CONTENT.md` credits "SSO (WorkOS)" 3× when shipped SSO is direct OIDC. The last needs your deliberate copy edit + landing redeploy.
