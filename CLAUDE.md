# CLAUDE.md — tela

Working context for tela. Read before contributing. Full architecture is in [`docs/`](docs/) and the deepest subsystem detail in [`docs/architecture.md`](docs/architecture.md); this file is the conventions + hard rules + the things that bite.

## What tela is

A self-hostable, markdown-native team wiki: Go + PostgreSQL backend, React/TS frontend with a Milkdown editor, live Yjs collaboration, and a TypeScript MCP server so agents are first-class. Public face **https://telawiki.com** (served behind a TLS-terminating reverse proxy; the standalone stack publishes Caddy on `:8780`). `pages.body` is canonical markdown forever — there is **no block table**.

> History: tela was built by an autonomous agent ("forge"), now under conventional development. The old forge workspace is archived out-of-tree (reference only — this repo + git history are the source of truth). Research/design docs from that era live in that archive and were never committed here.

## Layout

- `backend/` — Go. Module `github.com/zcag/tela/backend`, entry `cmd/tela`. `internal/{api,auth,db,mdimport,models,testdb}`. **PostgreSQL** via the `pgx/v5` stdlib driver — DB access is hand-written `database/sql` (positional `$1` placeholders), **no sqlc, no ORM**. Migrations are embedded `NNNN_name.sql` files (forward-only, no down) in `internal/db/migrations`, run automatically by `db.Migrate()` on boot. `0001_init.sql` is a Postgres baseline squashed from the retired SQLite migration history (the live DB held no data worth keeping). Datetimes are TEXT in `'YYYY-MM-DD HH:MM:SS'` UTC (default `tela_now()`), booleans are INTEGER 0/1 — both kept from the SQLite era so Go scans + the frontend `parseSqliteTs` path are unchanged.
- `frontend/` — React 19 + Vite + TS + Tailwind v4 + Radix + Milkdown (`@milkdown/kit`) + TanStack Query + TanStack Router + Orama + cmdk + Lucide + Storybook. `src/{components,lib,routes,styles}` + `App.tsx`/`main.tsx`. State is TanStack Query.
- `mcp/` — **thin stdio↔HTTP proxy** to the backend's built-in MCP server (`/api/mcp`). As of v0.7 the tool/resource surface lives in the Go backend (`internal/api/mcp*.go`), NOT here — this package is a dumb pipe that forwards the MCP protocol over stdio with the PAT as a bearer header (so there's no second implementation to drift). Published as `tela-mcp` on npm. Modern hosts skip it and use HTTP transport directly. See `mcp/README.md` + `docs/mcp-rewrite.md`.
- `deploy/` — docker-compose + `proxy/Caddyfile`. `.env` is gitignored (narrow line, not `*.env`); `.env.example` is committed.
- `landing/` — standalone **marketing landing page** (Astro + Tailwind v4 + OKLCH tokens, self-hosted Geist). Separate static build from the app; `backend/`+`frontend/` are untouched. Locked contracts at repo root: `CONTENT.md` (copy), `DESIGN.md` (look), `ACCEPTANCE.md` (gates). Targets: `make landing-dev` / `landing-build` / `landing-gate`. Tokens in `landing/src/styles/tokens.css` are its own source of truth — never hardcode color/px (the token-conformance gate enforces it). See `landing/README.md`. Caddy serves `landing/dist/` at the apex `/` (the app keeps `/login`, `/spaces`, `/share/*`, `/api/*`); ship it with `make deploy-landing` (split shape: builds, rsyncs `landing/dist/` to the box, reloads the external edge; the standalone stack instead bakes the landing into the proxy image — recreate via `make up`).

## Conventions

- **No issue/task tracker.** Do NOT open GitHub issues (or any other tickets) for this repo, ever. The `#NNN` references in older commits are artifacts from a retired system (forge) — do not continue or imitate them. Commit format is `type(scope): summary` (e.g. `feat(backend): hybrid chunk search`), no issue number. Concise messages, no co-author trailer.
- **Backend:** hand-written SQL via `database/sql`. New migration = new `NNNN_name.sql` (never edit an applied one; forward-only). One handler file per resource in `internal/api`.
- **Frontend hard rules (load-bearing):**
  1. Design tokens in `src/styles/tokens.css`, semantic names only. **Never** hardcode hex / raw px / ad-hoc radii.
  2. Theming via CSS custom properties on `[data-theme="..."]` — runtime switch, no rebuild.
  3. Radix + shadcn-style **owned** components only (`src/components/ui/`). No MUI/Chakra/Mantine/Ant/daisyUI.
  4. `@layer tokens, base, components, utilities` ordering is locked.
  5. Every new UI element uses owned primitives + tokens; missing primitive → build it (with a Storybook story) first.
  6. **Yjs is scoped:** imports allowed ONLY in `src/lib/collab/*` and the collab branch of `milkdown-editor.tsx`. Everything else explores pure-markdown / pure-SQL first.
- **MCP:** tools live in the backend (`internal/api/mcp_tools.go`) and call the same `xCore` funcs the REST routes do; row-returning tools wrap the row in a named envelope (`{ page: ... }`, `{ space: ... }`, `{ comment: ... }`, `{ feedback: ... }`) as typed output; sentinel returns use `{ ok: true }`. Per-tool scope via `mcpRequireWrite`; `/api/mcp` is on `IsPublicPath` and self-authenticates (bearer verifier). The npm `tela-mcp` proxy publish flow + hazards are in `docs/architecture.md` (MCP section).
- **Public docs are part of "done" (two doc surfaces — don't conflate them):** repo `docs/` is **developer/architecture** docs (design, internals, ops, gotchas). The **tela Docs space** — tela space **16** (slug `docs`), the user-facing product docs — is **end-user** docs. When you ship or change anything a tela **user** sees or configures (UI, settings, auth/login, sharing, sync, custom domains, MCP tools, …), updating the tela Docs space is part of shipping it, alongside any repo `docs/` change. Use the tela MCP (`list_pages`/`get_page`/`update_page`/`create_page` on space 16) to find the right page and keep it current; add a page if none fits. Skip only for purely internal/dev-facing changes with no user-visible surface.
- **Block authoring manifest (one source for slash menu + agent guide):** the authorable block palette lives in `frontend/src/components/app/blocks-manifest.json` — the **single source of truth**. The editor slash menu (`milkdown-slash.tsx`) *projects* it (joins an insert fn by `id`); the backend renders it into the MCP authoring guide (`mcp_authoring.go`: server Instructions + `create_page`/`update_page` tool hints + the `tela://authoring-guide` resource) from a generated `backend/internal/api/blocks_gen.json` (go:embed can't cross into `frontend/`). Edit the JSON, then `make blocks-gen`. `make blocks-gate` (run by `make test`) fails if the backend copy is stale OR a `milkdown-*` renderer plugin has no manifest entry / infra classification — so a new block can't ship invisible to agents or missing from the palette. Full design + "adding a block" steps in `docs/agent-authoring.md`. This is *why* agent-written pages were flat: the rich palette was never disclosed to the agent.

## Run / dev

```bash
make dev        # backend :8080 + frontend :5173 (vite proxies /api → :8080); boots a local dev Postgres
make test       # backend tests against a throwaway Postgres (boots dev-db, see Tests)
make storybook  # component dev surface
make up         # full docker stack on :8780 (prod-like; needs deploy/.env)
```

The backend requires **Postgres** — `make dev` / `make be-dev` boot a local container (`tela-dev-pg` on :55433, via `make dev-db`) and point `TELA_DATABASE_URL` at it. The schema is created/migrated on boot by `db.Migrate()`. Backend config is env-driven: **`TELA_DATABASE_URL`** (`postgres://user:pass@host:5432/db?sslmode=disable`), `TELA_PUBLIC_BASE_URL`, `TELA_SHARE_SECRET`, `TELA_API_KEY_SECRET`, `TELA_ADMIN_USERNAME/PASSWORD/EMAIL`, `TELA_SMTP_*`. In the docker stack `TELA_DATABASE_URL` is auto-built from `TELA_PG_USER/PASSWORD/DB`; see `deploy/.env.example`.

**Auth is email-first** (`internal/api/auth_register.go`, `internal/mailer`): open self-registration (`POST /api/auth/register`) → email confirmation (`verify-email`, signs the user in) → login by **email or username** (`Login` accepts `identifier`; an account with an unconfirmed email is blocked with `403 email_unverified`). Password reset via `request-password-reset` / `reset-password` (always-202 on request, no enumeration). Email goes through a provider-agnostic SMTP `mailer.Mailer`; with `TELA_SMTP_HOST` unset it falls back to logging the link (dev/first-boot). `users.email` is nullable — legacy/bootstrap username-only rows skip the email gate. Tokens (`email_tokens`) are stored hashed; the raw token lives only in the link. Verify/reset emails are branded HTML (inline hex, not tokens — email clients can't do OKLCH/CSS-vars).

## Tests

- Backend: `make test` (boots the dev Postgres, then `go test ./...`). Or run `go test ./...` directly with `TELA_TEST_DATABASE_URL` set to a maintenance DSN (a reachable db like `postgres`). Each test gets its own throwaway database via `internal/testdb.New(t)` (CREATE DATABASE → migrate → drop on cleanup) — full isolation, and the old `:memory:`-is-per-connection hazard is gone (a pool against one real PG is shared across connections). HTTP tests via `Handler(d)` + helpers `newWiredServer(t)`, `loginClient`, `newWiredServerOnDisk` (the on-disk variants are now aliases — kept for callers).
- MCP: the backend MCP surface is tested in Go (`backend/internal/api/mcp_test.go`, run by `make test`). The `mcp/` proxy has one live E2E (`npm run test:integration`, needs a running backend; `make test-mcp-integration` boots one). CI runs the integration suite.
- Frontend: vitest **browser-mode** is configured (`vite.config.ts`, `@storybook/addon-vitest` + Playwright provider — runs Storybook stories as tests), but there are **no standalone unit tests yet**. New FE unit tests are feasible; add them as vitest files or story-based tests rather than bouncing the brief.

## Gotchas (learned the hard way — full list in docs/architecture.md)

- **Two deployment shapes, one codebase.** (1) **Standalone** (`deploy/docker-compose.yml`, `make up`) — all-in-one; the `proxy` service is the edge and publishes Caddy on `:8780`. (2) **Split** (`deploy/docker-compose.split.yml`) — for hosts that already run a **shared reverse proxy** (one Caddy per box owns `:443`): there is no tela `proxy` container; the external edge `import`s `deploy/proxy/sites.caddy` (upstreams + landing root are env placeholders → the edge points them at the published loopback host ports), backend + frontend publish on **loopback** ports, and gotenberg can be a **remote** renderer over your private network. Org **custom domains** require this direct-TLS edge (on-demand cert issuance); behind an external terminator that owns TLS, on-demand never fires. `deploy/.env` on the deploy host holds all config + secrets.
- **Deploy** (`make` targets, runnable from any machine; configure `REMOTE`/paths in `deploy/deploy.env`, secrets in `deploy/.env`): **`make deploy` is the one command** — build the two images locally, push only changed layers to an **on-box registry** (`docker-compose.registry.yml`, loopback-bound) over a transient SSH tunnel, sync the landing + `sites.caddy`, recreate the split stack (box pulls the just-pushed `:<commit>` tag), health-gate `/api/version`. Fast partials: `deploy-backend` / `deploy-frontend` / `deploy-deck` (deck render sidecar — the fast path for a `slidev-theme-tahta` bump) / `deploy-landing`. `make deploy-offline` is the no-registry fallback (full-image `docker save | ssh docker load`) for first-boot/registry-down. The box only ever pulls — it never builds. Move the registry elsewhere (e.g. GHCR) via `TELA_REGISTRY` + `REG_TUNNEL=0`. Full design in `docs/deploy.md`.
- **Commit ≠ deploy:** pushing does not deploy — a `make deploy*` target does. The health gate catches a stale binary; if you deploy by hand, still `curl -s <PUBLIC_URL>/api/version` and compare `commit` to `git rev-parse --short HEAD` before claiming "live-verified".
- **`/api/version` is the BACKEND build — it says nothing about the frontend bundle.** The two images ship separately (`deploy-backend` / `deploy-frontend`), so a frontend-only change can be absent from the box while `/api/version` reports the very commit that contains it — and the reverse after a `deploy-frontend`. Reading it as "the deploy" makes a live frontend regression look shipped. To verify the **frontend**, compare content hashes: `npm run build` locally at the commit under test, then `curl -sL <PUBLIC_URL>/login | grep -oE '/assets/index-[A-Za-z0-9_-]+\.js'` — Vite hashes on content, so an identical `index-*.js` name means the served bundle IS that source. (A `grep -F` for a distinctive string of the change works too, but only proves presence, not that nothing else is stale.)
- **Secrets must be set and stable:** missing `TELA_API_KEY_SECRET`/`TELA_SHARE_SECRET` silently defaults to blank (compose only warns) → forgeable tokens. `TELA_PG_PASSWORD` has **no default** (compose fails without it). Rotating the secrets invalidates outstanding PATs / share cookies. Diff `deploy/.env` against `.env.example` after any refresh.
- **Public-path bypass:** `auth.IsPublicPath` is a `HasPrefix` check over `/share/`, `/p/`, `/api/share/`, `/api/public/`, `/api/diagrams/`, and `/dav/` (WebDAV sync — self-authenticates via PAT-as-Basic, scope-gated per verb in `DAVHandler`; see `docs/webdav-sync.md`). Any new route under these prefixes bypasses session middleware — it MUST self-authenticate.
- **Public spaces (blog surface):** `spaces.visibility` (`private`|`public`, migration `0012`) makes a *whole* space readable with no login. Public read-only API under `/api/public/` (`public_spaces.go`, `public_users.go`) self-authenticates on `visibility='public'`; it's GET-only and **publishing grants no write** (adds no `space_access`) — keep it that way. SPA routes `/public/spaces/{id}` (front-page index) + `/public/spaces/{id}/pages/{id}/{slug}` (reader) + `/u/{handle}` (user home) are unauthenticated children of `rootRoute`; `/p/{id}` redirects public-space pages to the reader. Full design + the SEO follow-up are in `docs/public-spaces.md`. Owner-only flip via `PATCH /api/spaces/{id}` `{visibility}`.
- **Sync filename ≠ slug (migration `0013`):** a page's /dav/ on-disk name is `pages.filename ?? slugify(title)` (`pageFileBase`, shared by the WebDAV tree *and* the zip export — keep them on it). `filename` is stamped server-side from the PUT path on sync-create and updated on a file rename, so a created file round-trips on rclone's post-PUT read-back (no 404→retry→**duplicate-page storm**) and survives retitles. It governs ONLY the sync surface — the URL slug stays title-derived (`pagemd.Encode`), never freeze it to the filename. NULL → falls back to the slug (app-made pages, legacy vaults). See `docs/webdav-sync.md`.
- **XFF / rate-limit:** Caddy is the only trusted upstream (`trusted_proxies static private_ranges`); backend reads the **rightmost** XFF hop, not leftmost.
- **Go 1.22+ ServeMux** rejects a literal segment after a wildcard (`/api/foo/{x}/literal`). Test new wildcard routes locally.
- **pgx is strict about types:** a SQL `boolean` expression scanned into a Go `int` errors (SQLite silently returned 0/1). Return `CASE WHEN … THEN 1 ELSE 0 END` for INTEGER-boolean columns, or scan into `bool`. Same for any implicit text↔int comparison SQLite tolerated.
- **Search is ranked Postgres FTS:** `/api/search` + `/api/search/bodies` run ranked Postgres full-text over `pages.search_tsv` (`ts_rank_cd`, `ts_headline`, `websearch_to_tsquery`; migration `0004_pages_fts.sql`) — the old unranked `ILIKE` placeholder is **gone**. The semantic-enrichment tier (RAG over `pgvector`) is partially wired; the full two-tier instant+semantic design is in `docs/search.md`.
- **RAG embedder is a remote Ollama (`TELA_RAG_EMBED_URL`):** the `/api/rag/*` + MCP `semantic_search` path embeds via an external Ollama; with the var unset the feature no-ops (503), so it's an operational dependency, not in-process. Point it at a *dedicated embed-only* Ollama instance (model **`qwen3-embedding:0.6b`**, 1024-d — matches the `page_chunks.embedding vector(1024)` column; pin `keep_alive=-1` so it stays warm). **Gotcha — names vs IPs:** if the embed host is reached over a private overlay network (e.g. a VPN/tailnet) and the backend runs in Docker, the overlay **hostname may not resolve inside the container** — use the host's overlay **IP** in `TELA_RAG_EMBED_URL`, not the name. If RAG/semantic search 503s or hangs, check the instance is up (`curl <embed-host>:<port>/api/ps`). **Changing the model = re-embed everything:** the chunk hash folds in the model name, so after editing `TELA_RAG_EMBED_MODEL` + redeploying the backend, run `… exec backend /tela reindex-all` (resumable).
- **URL-unfurl fetch SSRF:** the paste-a-link title fetch (`/api/unfurl`, `unfurl.go`) hits arbitrary user-supplied URLs, so it's https/http-only, rejects any dial to a private/loopback/link-local IP at connect time (closes the DNS-rebind window), and follows **no** redirects — never re-enable redirects or drop the dial-time IP guard.
- **FE public/share hooks use raw `fetch()`, not `api()`** — `api()` redirects to `/login` on 401, but in share-mode 401 means "password required".
- **Inline math is `$$…$$`, single-`$` math is OFF** (`frontend/src/lib/markdown/math-options.ts`, shared by the view stack *and* the editor's `mathRemarkPlugin` — they must not drift). With remark-math's default on, two `$` in one paragraph pair into an `inlineMath` node and the prose between them silently vanishes from the render: `**$3,500/mo** ≈ **₺166,000** … $2,500` renders as one broken math span, while the markdown source looks fine. A *lone* `$` is harmless, so the edit that adds a second amount breaks text written weeks earlier — nothing at the source level catches it.
- **`patch_page` targets a section's whole SUBTREE** (`pagemap.go`): `replace`/`delete` on a `##` also drop its `###` children, `append` lands after the last of them. That's the standard span definition, but it's silent content loss through an agent tool, so the result carries `removed_subsections` + a `warning` — keep it that way if you touch `applyPatch`. Headings inside a blockquote (`> ### …` — the callout the authoring guide recommends for "do this now") ARE addressable; patched content is re-prefixed with the quote marker so it stays inside the callout.
- **Heading anchor ids come from `MarkdownView`, and the hash comes from the ROUTER.** `stampHeadingIds` (`lib/markdown/heading-anchors.ts`) gives every rendered heading a `pageSlug` id, so `#some-section` works on every surface; `ReaderShell` builds its TOC from the same helper — don't re-derive slugs anywhere else or in-body TOC links break on one surface only. For a deep-link jump, read the hash from `useLocation`, **never `window.location.hash`**: on a fresh load the router normalizes the URL *after* the first render, so a render-time read sees `""` and the jump silently no-ops. Debugging note: `ReaderShell`'s post-render pass runs inside `requestAnimationFrame`, which browsers **pause in a background tab** — a headless check driving a non-foreground tab sees no ids, no TOC and no anchors, which looks exactly like a broken build (it isn't; focus the tab).
- **Milkdown `SlashProvider` debounce wedges under React+Yjs** — don't drive `provider.update()` from a render effect; manage `dataset.show` + position manually (see architecture.md).
- **Public URLs are canonical at `/{handle}/{space-slug}[/{pageId}/{title-slug}]`, and the route shape is duplicated in TWO proxies.** The owning handle is an **org's slug** when the space is org-owned, else the personal owner's username — defined once as `spaceHandleExpr`/`handleSpacePath` (`public_handles.go`). Re-deriving it as "the `space_members` owner" is the bug that keeps coming back: it attributed every public ORG space to whoever created it, on the handle home, the sitemap, the `/u/` OG cards, discover bylines, and the by-handle payload the reader builds its whole nav from. Clients never derive it — spaces carry a server-computed `public_path`. The page **id stays load-bearing** so the trailing slug is decorative and a rename never breaks a shared link; `/public/spaces/{id}/…` stays live and canonicalizes to the pretty form. **The trap:** the path regex lives in BOTH `deploy/proxy/sites.caddy` (`@handle`) and `frontend/nginx.conf` — and nginx serves the SPA shell as its **404 body**, so a URL shape missing from its allowlist *renders perfectly while returning 404*. It looks fine in a browser, no test catches it, and only crawlers and monitors see the dead URL. Verify new public URL shapes with `curl -o /dev/null -w '%{http_code}'`, not by eye.
- **Two scroll models, gated on `pointer: coarse`:** desktop is a fixed-height shell (`h-dvh`, chrome pinned, content scrolling inner `overflow-y` frames); **touch releases every frame** (shell + `<main>` in `router.tsx`, the page view/edit bodies in `PageView.tsx`, `.reader-scroll`) to natural **document scroll**. Reason: Firefox/Chrome Android only visual-viewport-pan a pinch-zoomed page over the *document* — never over an inner scroll frame — so a zoomed-in user couldn't drag the page sideways at all (iPad Safari pans natively regardless, so it looks fine there and hides the bug). Don't wrap doc content in a new scroll container without a coarse escape, and don't "tidy away" the coarse overrides — either silently kills one-finger pan when zoomed, and no test catches it. The `min-height: 100dvh` floor on `html/body/#root` is load-bearing: it keeps fixed-size widgets (sheets, graphs) bounded to the viewport while letting tall prose grow into document scroll. **The release assumes the *document* takes over — which a `position: fixed` ancestor breaks.** A fixed box can't hand its overflow to the document, so releasing an inner frame inside one leaves *nothing* scrollable: that's how the authed `?view=read` reader (a `fixed inset-0` overlay in `PageView`) was stuck on its first screenful on touch while the same `ReaderShell` scrolled fine on `/share`. A fixed overlay must opt into being the scroller itself (`pointer-coarse:overflow-y-auto`). Anything reading a frame's `scrollTop` must therefore *resolve* what actually scrolls rather than assume `window` on coarse — `ReaderShell` walks up for the nearest `overflow-y: auto|scroll` ancestor and falls back to `window`; a wrong guess silently freezes the progress bar and TOC scroll-spy. No test catches any of this.
- **The reader silently drops things the markdown implies — that's what `lint_page` is for.** `MarkdownView`'s `case 'html': return null` drops ALL raw HTML but the `<details>` collapsible form, so `<br>`, `<img>`, `<span style>` and prose placeholders like `<org>`/`<commit>` (CommonMark parses those as tags) vanish while the text *between* them survives — the loss reads as normal prose. An unrecognized `:::name` or `[!TYPE]` unwraps to bare content. A `<details>` without blank lines around its body is merged into one raw-html node and dropped **whole**. None of it is visible from the source, which is the point: `backend/internal/pagelint` (structural, pure text) + `page_lint.go` (wikilink/link/attachment resolution) report it, `preview_page` renders the page through gotenberg and hands back the text. Advisory everywhere — unlike `deckWriteGate`, no page write is ever rejected. The directive/callout vocabulary is GENERATED from the frontend transforms into `blocks_gen.json` by `make blocks-gen`; never hand-copy it into Go. **When adding a rule, verify its claim against the live reader, not against your reading of the renderer** — `ragged-table` first shipped a message saying the extra cells were dropped (what the GFM spec says); the reader actually renders them as a stray column. Full design in `docs/page-lint.md`.
- **MCP release hazards:** verify `npm version` actually created the commit+tag; `bin` paths must not start with `./` (npm strips them); ESM entrypoint guard must `realpathSync` (npx symlinks). Details in architecture.md.
- **Decks (Slidev):** a deck is a page with `props.deck=true`; its body is Slidev markdown stored **verbatim** (don't strip the leading frontmatter — it's headmatter, not page props). The look is the `slidev-theme-tahta` npm package (tela injects theme/themeConfig/`mdc:true`; never hardcode visuals). **Present = live SPA** served membership-gated at `/api/pages/{id}/deck/spa/{path...}`; **gotcha:** Slidev doubles a non-root `--base` on programmatic nav → the injected `setup/main.mjs` router guard (`SPA_NAV_FIX`) is load-bearing. Cache key folds in the theme version (a tahta bump auto-rebuilds). The agent guide is a **pass-through of tahta's `AGENTS.md`** — change deck authoring richness in **tahta**, not tela; it flows through on bump. Full design in `docs/deck.md`.
